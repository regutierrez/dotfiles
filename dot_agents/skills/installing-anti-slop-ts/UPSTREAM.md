# Upstream provenance

- Repository: https://github.com/dmmulroy/anti-slop
- Imported commit: `c44ef22ca116d0ba62a3ff663a0bd13a3f3fa40b`
- Imported path: `skills/install-anti-slop/`
- Local changes: renamed the skill and heading to `installing-anti-slop-ts` so TypeScript/Oxlint and Python tooling have distinct entry points, and copied the upstream repository's MIT license into this skill. Bundled plugin names and installation behavior are unchanged.

Treat `src/` in the upstream repository as canonical. Its skill assets are generated from that source. For an update, resolve and review an immutable upstream revision, compare the full skill bundle, preserve this local naming distinction, and record the new commit here.
